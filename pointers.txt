/*
Points are variables that store memory addresses. Normally, a variable directly contains a specific value.
A pointer contains an address of a variable that contians a specific value.
Referencing a vlaue through a pointer is called INDIRECTION.

Declaring Pointers:
    Pointers, like all variables, must be defined w/ a data type before they can be used.
        int count;
        int *countPtr;
    Specifies that variable counPtr is of type int * (i.e., a pointer to an integer) and is read (right to left)

    Operators:
        & is the address operator, returns the addressof it operand.
        I.e.,
            int y = 5
            int *yPtr;

Dereference a pointer using a *
    ie: *d =  2

Constant Pointer: Can change the address you're storing and the value that it is pointing to.
    ie: const double * v; (//You cannot change the value.)

Indirection Operator (*): returns the value of the object to which it points.
    ie: std::cout << *yPtr:
    Prints the value of variable y

Initializing Pointer
    Always initilize pointer to prevent errors
    Dereferencing a pointer that is not initilaized can cause runtime errors and modify data.
    C++11 introduce nullptr





Arrays & Pointers

A C++ array name is actually a constant pointer to the first element

Pointers to void (can hold address of any type and can be typecasted to any type)
    void* is a generic pointer that cna hold the address of any data type.
    It cannot be dereferenced directly

Pointers to functions (pass a funciton to another funciton, create and array of funcitons)

Arrays of Pointers (to store the strings)

DYnamic memory allocation (allocate a new memory run time of a program)



*/